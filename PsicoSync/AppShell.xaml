<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="PsicoSync.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:PsicoSync"
    xmlns:views="clr-namespace:PsicoSync.Views"
    Shell.FlyoutBehavior="Disabled"
    Title="PsicoSync">

    <TabBar>
        <Tab Title="Citas" Icon="clipboard.png">
            <ShellContent
            ContentTemplate="{DataTemplate views:CitasPage}"
                Route="CitasPage"/>
        </Tab>
        <Tab Title="Resumen" Icon="resumen.png">
            <ShellContent
            ContentTemplate="{DataTemplate views:ResumenPage}"/>
        </Tab>
        <Tab Title="Catálogos" Icon="catalogo.png">
            <ShellContent
            ContentTemplate="{DataTemplate views:CatalogosPage}"/>
        </Tab>
        <Tab Title="Config" Icon="config.png">
            <ShellContent
            ContentTemplate="{DataTemplate views:ConfigPage}"/>
        </Tab>
    </TabBar>

</Shell>
